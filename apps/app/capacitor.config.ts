import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fuocoappdev.app',
  appName: 'fuoco.appdev',
  webDir: '../../dist/apps/app',
  bundledWebRuntime: false,
};

export default config;
